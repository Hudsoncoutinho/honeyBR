apiVersion: v1
kind: Service
metadata:
  name: honeybr-dashboard
spec:
  type: "{{ .Values.service.type }}"
  selector:
    app: honeybr
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
